{
	"extends": "../.templates/tsconfig.json",
	"compilerOptions": {
		// "module": "CommonJS", // do not >:( WebPack will handle this.
		"target": "es2019", // can use es2020 for node 14.
		"lib":   ["es2019"], // no dom.
		"types": ["my-type-utils", "node", "express"],
	},
	"references": [
		{ "path": "../base" },
	],
}
