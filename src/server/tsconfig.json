{
	"extends": "../tsconfig-childbase.json",
	"compilerOptions": {
		// "module": "CommonJS", // do not >:( WebPack will handle this.
		"target": "es2020", // can use es2020 for node 14.
		"lib":   ["es2020"], // no dom.
		"types": ["my-type-utils", "node"],
	},
	"references": [
		{ "path": "../base" },
	],
}
